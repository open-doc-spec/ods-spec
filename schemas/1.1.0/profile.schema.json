{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/open-doc-spec/ods-spec/main/schemas/1.1.0/profile.schema.json",
  "title": "ODS Custom Profile Definition Schema (v1.1.0)",
  "description": "Machine-readable JSON Schema for custom profile declarations in ODS (ods.custom_profile) in v1.1.0.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Unique lowercase identifier for the profile (e.g. 'okf', 'rfc', 'ontology', 'memory-episode')."
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of what this profile represents and when to use it."
    },
    "required_sections": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of Markdown H2/H3 heading titles that MUST be present in conforming documents."
    },
    "optional_sections": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of Markdown H2/H3 heading titles that MAY be present in conforming documents."
    },
    "required_keys": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of frontmatter keys required for this profile."
    },
    "forbidden_keys": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of frontmatter keys that are forbidden for this profile."
    }
  },
  "required": [
    "name",
    "required_sections"
  ],
  "additionalProperties": false
}
